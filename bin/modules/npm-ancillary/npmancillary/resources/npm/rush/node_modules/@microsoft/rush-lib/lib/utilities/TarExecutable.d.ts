import { Terminal } from '@rushstack/node-core-library';
import { RushConfigurationProject } from '../api/RushConfigurationProject';
export declare class TarExecutable {
    private _tarExecutablePath;
    private constructor();
    static tryInitialize(terminal: Terminal): TarExecutable | undefined;
    /**
     * @returns
     * The "tar" exit code
     */
    tryUntarAsync(archivePath: string, outputFolderPath: string): Promise<number>;
    /**
     * @returns
     * The "tar" exit code
     */
    tryCreateArchiveFromProjectPathsAsync(archivePath: string, paths: string[], project: RushConfigurationProject): Promise<number>;
}
//# sourceMappingURL=TarExecutable.d.ts.map